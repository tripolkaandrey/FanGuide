@model FanGuide.ViewModels.TeamFormViewModel
@{
    ViewBag.Title = "TeamForm";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Add a new team</h2>

@using (Html.BeginForm("Create", "Teams"))
{
    @Html.ValidationSummary(true)
    <div class="form-group">
        @Html.LabelFor(x => x.Team.Name)
        @Html.TextBoxFor(x => x.Team.Name, new { @class = "form-control" })
        @Html.ValidationMessageFor(x => x.Team.Name)
    </div>
    <div class="form-group">
        @Html.LabelFor(x => x.Team.SportId)
        @Html.DropDownListFor(x => x.Team.SportId, new SelectList(Model.Sports, "Id", "Name"), "Select Sport", new { @class = "form-control" })
        @Html.ValidationMessageFor(x => x.Team.SportId)
    </div>
    @Html.HiddenFor(x => x.Team.Id)
    <button type="submit" class="btn btn-primary">Save</button>
}

@section scripts
{
    @Scripts.Render("~/bundles/jqueryval")
}
